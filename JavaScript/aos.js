AOS.init({
    offset: 120, //Kích hoạt thời gian chuyển động chậm hơn mặc định, mặc định 120
    duration: 700, //Thời gian của hiệu ứng chuyển động 
    easing: 'ease-in-sine', //Dùng để điều khiển chức năng tạo hiệu ứng chuyển động cho các đối tượng HTML
    delay: 100, //Time trì hoãn chuyển động
  });